@model DJRM.Configuration.Model.Task

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Task</legend>

		@Html.Partial("_CreateOrEdit", Model)
        
    </fieldset>
    
    <input type="submit" value="Create" />
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


